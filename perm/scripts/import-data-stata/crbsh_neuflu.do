clear
import delimited "../data-csv/crbsh_neuflu.csv", bindquote(strict)

label variable id_addi "Masked Participant ID for ADDI"

label variable flucat "CategFluency - mean(fruit, animal, veg)"

label variable flulet "LettrFluency - mean(F, A, S)"


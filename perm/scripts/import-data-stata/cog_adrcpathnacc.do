clear
import delimited "../data-csv/cog_adrcpathnacc.csv", bindquote(strict)

label variable id_addi "Masked Participant ID for ADDI"

label variable cdagedx "Age of DX of cog impairment"

label variable cdageos "Age of onset of cog impairment"

label variable cdpos "Pattern of onset"

label variable cdprog "Pattern of progression"

label variable nac1 "Responses based on"

label variable nac10 "Undetermined etiology"

label variable nac10a "Undetermined etiology - If present"

label variable nac11 "Frontotemporal dementia"

label variable nac11a "Frontotemporal dementia - If present"

label variable nac12 "Primary progressive aphasia"

label variable nac12a "Primary progressive aphasia - If present"

label variable nac12a1 "Progressive nonfluent aphasia (1=present, 0-=absent)"

label variable nac12a2 "Semantic dementia (1=present, 0-=absent)"

label variable nac12a3 "Semantic dementia - variant (1=present, 0-=absent)"

label variable nac12a4 "Other (1=present, 0-=absent)"

label variable nac13 "Progressive supranuclear palsy"

label variable nac13a "Progressive supranuclear palsy - if present"

label variable nac14 "Corticobasal degeneration"

label variable nac14a "Corticobasal degeneration - if present"

label variable nac15 "Huntington's"

label variable nac15a "Huntington's - if present"

label variable nac16 "Prion disease"

label variable nac16a "Prion disease - if present"

label variable nac17 "Cognitive dysfunction meds"

label variable nac17a "Cognitive dysfunction meds - if present"

label variable nac18 "Cognitive dysfunction medical illness"

label variable nac18a "Cognitive dysfunction medical illness - if present"

label variable nac19 "Depression"

label variable nac19a "Depression - if present"

label variable nac2 "Normal cog"

label variable nac20 "Other major psychiatric illness"

label variable nac20a "Other major psychiatric illness - if present"

label variable nac21 "Down's syndrome"

label variable nac21a "Down's syndrome - if present"

label variable nac22 "Parkinson's disease"

label variable nac22a "Parkinson's disease - if present"

label variable nac23 "Stroke"

label variable nac23a "Stroke - if present"

label variable nac24 "Hydrocephalus"

label variable nac24a "Hydrocephalus - if present"

label variable nac25 "Traumatic brain injury"

label variable nac25a "Traumatic brain injury - if present"

label variable nac26 "CNS neolasm"

label variable nac26a "CNS neolasm - if present"

label variable nac27 "Other"

label variable nac27a "Other - specify"

label variable nac27as "NAC27AS"

label variable nac28 "ALS"

label variable nac28a "ALS - if present"

label variable nac29 "HIV encephalopathy"

label variable nac29a "HIV encephalopathy - if present"

label variable nac3 "Meet criteria for dementia"

label variable nac30 "Hippocampal sclerosis"

label variable nac30a "Hippocampal sclerosis - if present"

label variable nac31 "Hypoxic/anoxic"

label variable nac31a "Hypoxic/anoxic - if present"

label variable nac32 "Delirium"

label variable nac32a "Delirium - if present"

label variable nac33a "Hypertension"

label variable nac33aa "Hypertension - if present"

label variable nac33b "Heart failure"

label variable nac33bb "Heart failure - if present"

label variable nac33c "Diabetes"

label variable nac33cc "Diabetes - if present"

label variable nac33d "COPD"

label variable nac33dd "COPD - if present"

label variable nac33e "Cancer"

label variable nac33ee "Cancer - if present"

label variable nac33f "Sleep apnea"

label variable nac33ff "Sleep apnea - if present"

label variable nac33g "Alcoholism"

label variable nac33gg "Alcoholism - if present"

label variable nac33h "Drug use disorder"

label variable nac33hh "Drug use disorder - if present"

label variable nac33i "Anemia"

label variable nac33ii "Anemia - if present"

label variable nac33j "Arthritis"

label variable nac33jj "Arthritis - if present"

label variable nac33k "Macular degeneration"

label variable nac33kk "Macular degeneration - if present"

label variable nac33l "Other psychiatric disorder"

label variable nac33ll "Other psychiatric disorder - if present"

label variable nac34 "NAC34"

label variable nac35 "NAC35"

label variable nac36 "NAC36"

label variable nac37 "NAC37"

label variable nac38 "NAC38"

label variable nac39 "NAC39"

label variable nac40 "NAC40"

label variable nac41 "NAC41"

label variable nac4a "Amnestic MCI - impairment only"

label variable nac4b "Amnestic MCI - domains"

label variable nac4b1 "NAC4B1"

label variable nac4b2 "NAC4B2"

label variable nac4b3 "NAC4B3"

label variable nac4b4 "NAC4B4"

label variable nac4c "Non-Amnestic MCI - single"

label variable nac4c1 "NAC4C1"

label variable nac4c2 "NAC4C2"

label variable nac4c3 "NAC4C3"

label variable nac4c4 "NAC4C4"

label variable nac4d "Non-Amnestic MCI - multi"

label variable nac4d1 "NAC4D1"

label variable nac4d2 "NAC4D2"

label variable nac4d3 "NAC4D3"

label variable nac4d4 "NAC4D4"

label variable nac4e "Impaired, not MCI"

label variable nac5 "Prob AD"

label variable nac5a "Prob AD - if present"

label variable nac6 "Possible AD"

label variable nac6a "Possible AD - If present"

label variable nac7 "Demenia w/ Lewy bodies"

label variable nac7a "Demenia w/ Lewy bodies - If present"

label variable nac8 "Vascular dementia"

label variable nac8a "Vascular dementia - If present"

label variable nac9 "Alcohol dementia"

label variable nac9a "Alcohol dementia - If present"

